<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Anime Emporium" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav style="background-color: #f8f9fa; padding: 10px 20px; box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1); display: flex; justify-content: space-between; align-items: center; font-family: 'Americana', serif;">
        <ul style="list-style: none; display: flex; gap: 30px; margin: 0; padding: 0;">
          <% Category.all.each do |category| %>
            <li>
              <%= link_to category.name, category_products_path(category), style: "text-decoration: none; color: #007bff; font-weight: 300; font-size: 18px;" %>
            </li>
          <% end %>
        </ul>

        <div style="font-size: 28px; font-weight: 500; text-align: center; letter-spacing: 2px; margin-left:-220px;">
          <%= link_to "Anime", root_path, style: "text-decoration: none; color: black;" %>
        </div>

        <ul style="list-style: none; display: flex; gap: 30px; margin: 0; padding: 0;">
          <% if customer_user_signed_in? %>
            <!-- Show user details if signed in -->
            <li>
              <%= link_to customer_user_path(current_customer_user), style: "text-decoration: none; color: black;" do %>
                <i class="fas fa-user" style="font-size: 24px;"></i>
              <% end %>
            </li>
          <% else %>
            <!-- Show login link if not signed in -->
            <li>
              <%= link_to "Login", new_customer_user_session_path, style: "text-decoration: none; color: black; font-size: 18px;" %>
            </li>
          <% end %>

          <li>
            <a href="#" style="text-decoration: none; color: black;">
              <i class="fas fa-heart" style="font-size: 24px;"></i>
            </a>
          </li>
          <li>
            <a href="<%= cart_path %>" style="text-decoration: none; color: black;">
              <i class="fas fa-shopping-bag" style="font-size: 24px;"></i>
            </a>
          </li>
        </ul>
      </nav>
    </header>

    <!-- Flash Messages -->
    <div id="flash-messages" style="position: fixed; top: 10px; left: 50%; transform: translateX(-50%); z-index: 1000; width: 90%; max-width: 500px;">
      <% flash.each do |key, message| %>
        <div class="flash-message <%= key %>" style="background-color: <%= key == 'notice' ? '#4caf50' : '#f44336' %>; color: white; padding: 10px 20px; margin-bottom: 10px; border-radius: 5px; text-align: center; font-weight: bold;">
          <%= message %>
        </div>
      <% end %>
    </div>


    <!-- Page Content -->
    <%= yield %>
  </body>
</html>


<script>
  document.addEventListener('DOMContentLoaded', () => {
    const flashMessages = document.getElementById('flash-messages');
    if (flashMessages) {
      setTimeout(() => {
        flashMessages.style.transition = 'opacity 0.5s ease';
        flashMessages.style.opacity = '0';
        setTimeout(() => {
          flashMessages.style.display = 'none';
        }, 500);
      }, 2000);
    }
  });
</script>
